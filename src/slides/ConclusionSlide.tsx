import React from "react";
import { Typography } from "@material-ui/core";

const ConclusionSlide = () => (
  <div className="slide">
    <Typography variant="h1">Conclusão</Typography>
    <Typography variant="body1">
      O React.js é uma poderosa biblioteca JavaScript para a criação de
      interfaces de usuário. Com o seu modelo de programação baseado em
      componentes, o uso do Virtual DOM e a adição dos Hooks, o React simplifica
      o desenvolvimento de aplicações web modernas e escaláveis.
      <br />
      <br />
      Além disso, o React possui uma comunidade ativa e um ecossistema rico de
      bibliotecas e ferramentas que facilitam o desenvolvimento de aplicações
      React.
    </Typography>
  </div>
);

export default ConclusionSlide;
